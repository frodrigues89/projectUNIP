const RelatorioController = {
    processarRelatorio: (dadosRelatorio: any) => {
      // Aqui você pode adicionar a lógica necessária para processar os dados do relatório
      // Por exemplo, calcular a média de solicitações ao longo do mês e verificar em relação à meta mensal
      return dadosRelatorio;
    },
  };
  
  export default RelatorioController;
  